server:
  port: 8189
  servlet:
    context-path: /app
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/online_store_db
    username: postgres
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect:
